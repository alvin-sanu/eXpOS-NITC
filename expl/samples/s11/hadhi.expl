type
student{
  str name;
  int marks;
}
endtype

int main() {
decl
  int temp, val;
  student a;
enddecl

begin
  val = initialize();
  a = alloc();
  breakpoint;
  a.name = "John";
  a.marks = 89;
  temp = exposcall( "Write", -2, a.name);
  temp = exposcall( "Write", -2, a.marks);
  free(a);
  return 0;
end
}
